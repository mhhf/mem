import 'org.sol';
import 'dapple/test.sol';
import 'dapple/debug.sol';

contract OrgTester is Test {
  
  Org org;
  function setUp() {
    org = new Org();
  }
  
  function testNewOrg(  ) {
    org = new Org();
    org.newOrg("test","abi");
    var (n,,,,) = org.info('test');
    assertTrue(n == 1);
  }
  
  function testPropose(  ) {
    
    org.propose('test', 'omg_candidate');
    // string consens = org.getConsens('test');
    // TODO - only bytesXX can be returned -> refactor org
    // bytes32 str = org.str();
    // assertEq(consens, 'omg_candidate');
    // assertTrue(consens);
    
  }

}
